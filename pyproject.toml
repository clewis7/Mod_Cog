[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mod-cog"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["neurogym>=2.3,<3"]

[tool.hatch.build.targets.wheel]
packages = ["Mod_Cog"]
